langcode: en
status: true
dependencies: {  }
id: cas_weather_monthly_data_group_content
migration_tags:
  - Drupal 7 OG
  - OSU Groups Content
  - OSU Groups
  - OSU
  - CAS
  - CAS Groups
migration_group: osu_groups
label: 'Migrate D7 weather_monthly_data nodes into Group content'
source:
  plugin: d7_og_membership_content
  node_type:
    - weather_monthly_data
process:
  label: etid
  type:
    plugin: default_value
    default_value: group_content_type_f85244078f7d2
  gid:
    - plugin: migration_lookup
      migration: cas_node_og_group
      no_stub: true
      source: gid
    - plugin: skip_on_empty
      method: row
      message: 'gid is missing'
  entity_id:
    - plugin: migration_lookup
      migration: cas_weather_monthly_data_to_weather_monthly_data
      no_stub: true
      source: etid
    - plugin: skip_on_empty
      method: row
      message: 'node missing.'
destination:
  plugin: 'entity:group_content'
migration_dependencies:
  required:
    - cas_node_og_group
    - cas_weather_monthly_data_to_weather_monthly_data
